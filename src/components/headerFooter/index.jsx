import React from "react";

const Header = () => {
	return <header id="page-header"> This is page header </header>;
};

const Footer = () => {
	return <footer id="page-footer"> This is page footer</footer>;
};

export { Header, Footer };
